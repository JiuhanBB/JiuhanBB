const id=new URLSearchParams(location.search).get("id")||"";const root=document.querySelector("#article");const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
async function load(){if(!/^[a-z0-9-]+$/i.test(id)){showError();return}try{const r=await fetch(`posts/${id}.json?t=${Date.now()}`);if(!r.ok)throw new Error();const p=await r.json();document.title=`${p.title} · 久寒包包`;const content=String(p.content||"").replace(/\\n/g,"\n");root.innerHTML=`<span class="tag">${esc(p.category)}</span><h1>${esc(p.title)}</h1><p class="meta">${esc(p.date)}</p><div class="article-body">${marked.parse(content)}</div>`}catch{showError()}}
function showError(){root.innerHTML='<div class="loading">这篇文章好像去了另一个次元。<br><a href="articles.html">返回全部文章</a></div>'}load();if(window.lucide)lucide.createIcons();
